export default function Verify() {
  return (
    <main style={{minHeight:"100vh", display:"grid", placeItems:"center", padding:24}}>
      <div style={{maxWidth:480}}>
        <h1 style={{fontSize:24, marginBottom:12}}>Verify your email</h1>
        <p>We sent you a verification link. After verifying, return to the app and log in.</p>
      </div>
    </main>
  );
}
